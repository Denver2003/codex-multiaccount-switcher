# Task 00 - Read Specs and Lock Scope

## Goal

Make the implementation scope explicit before code is written.

## Checklist

- [ ] Read `docs/mvp-spec-linux-macos.md`.
- [ ] Read `docs/development-spec-linux-macos.md`.
- [ ] Confirm the CLI-only MVP scope.
- [ ] Confirm the supported platforms are Linux and macOS.
- [ ] Confirm the storage layout and commit semantics.
- [ ] Confirm the active auth file path and profile store path.
- [ ] Confirm the `add`, `save-current`, `switch`, `rename`, `remove`, `status`, and `list` command set.
- [ ] Confirm the developer rules in `agents.md`.

## Output

- [ ] Short implementation note summarizing any remaining ambiguities.
- [ ] No code changes in this task unless a blocking spec issue is discovered.

